# 实施与验收清单

- [x] 1. Nuxt / Nuxt UI 基础、暖纸色设计、Git 初始化与构建验证。
- [x] 2. SQLite / Drizzle 项目、媒体导入、可编辑时间片段、渠道配置。
- [x] 3. 持久任务队列、并发设置、失败暂停 / 重试 / 跳过 / 继续、重启恢复。
- [x] 4. 视频抽轨、本地 Demucs 人声/背景分离、VAD 分段、Whisper 识别、翻译、火山音色参考合成。
- [x] 5. 按片段替换、背景保留、时长对齐、音视频导出、原始/配音对比。
- [x] 6. Electron 本地服务、macOS 安装包、更新下载/安装接口、网页热更新签名校验与原子切换。
- [ ] 正式发布验收：生产签名 / 公证、线上更新源和跨平台安装验证。
- [ ] 真实火山 / 翻译接口及影视素材的音色、背景听感验收（等待用户配置 Key）。
- [x] 7. 类型检查、队列与管线测试、真实 FFmpeg 媒体验证、界面验收与使用文档。

## 当前验收证据

- `npm run typecheck` 通过。
- `npm test`：21 项测试通过，覆盖字幕解析、流程起点、并发 / 依赖调度、变速对齐、Key 脱敏、真实 FFmpeg 音视频导出、片段替换与背景保留、翻译 / 合成错误、网页更新签名与原子切换，以及原声参考生命周期。
- `npm run build` 通过；Nuxt 产物包含 Nitro API 和本地数据库依赖。
- 浏览器实测文本项目导入、连续时间轴、片段翻译编辑、保存、任务入队和缺少 `VOLCENGINE_API_KEY` 的失败提示。
- 本机模型环境已安装；短语音已实际完成 Demucs 分离与 faster-whisper 识别。

## 发布前需要提供

发布签名证书、更新托管地址、真实火山 Audio / 翻译 Key、正式模型缓存和实际影视素材验收仍需在发布环境完成。没有这些外部资料，应用会明确显示配置缺失，不会伪造 AI 结果。

- `npm run test:integration`：5 项生产 HTTP 测试通过，覆盖并发重复入队、失败暂停/重试到音频导出、Range 请求、跨站/路径拒绝、应用重启恢复，以及签名网页更新即时生效。
- macOS Apple Silicon DMG 与 ZIP 已构建；打包后的 `.app` 已独立启动，未依赖项目目录中的服务。
- 真实音频通过 HTTP 导入并连续完成人声分离、VAD、Whisper 识别，缺少翻译 Key 时按预期暂停。
- 浏览器桌面及 390px 手机宽度已检查。
- 原声参考回归：未分离的音视频禁止纯文本降级；手动片段按起止时间截取参考；参考丢失时重建；重新分离成功使旧配音失效，失败保留已发布音轨。参考截取和配音落盘使用真实 FFmpeg，AI 响应和分离进程在回归测试中模拟，不作为真实音色质量的验收证据。
